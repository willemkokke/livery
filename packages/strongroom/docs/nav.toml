# This package's section of the workspace site, hand-authored.
# Labels map to pages relative to this docs/ tree, or to nested
# lists. Every authored page must appear here, and every entry must
# name a real page. A generator owns a block between `# nav:begin
# <name>` and `# nav:end <name>` markers (tasks, tools); the emitter's
# own sections, changelog, coverage and api, are blocks too: place a
# pair where one belongs, and a section without one lands after the
# authored entries, changelog and coverage ahead of the tasks block,
# api last.
nav = [
    { "Index" = "index.md" },
    { "Using the store" = "store.md" },
    { "The standard" = "standard.md" },
    { "Platforms" = "platforms.md" },
]
