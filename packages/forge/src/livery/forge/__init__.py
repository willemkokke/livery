"""One interface to GitHub, Gitea, and GitLab — the livery ecosystem's forge.

The protocol arrives with bootstrap phase 1; this release reserves the
name and proves the release train. Runtime dependencies: the standard
library, and nothing else — enforced by the workspace contracts test.
"""

from __future__ import annotations

__version__ = "0.0.1"

__all__ = ["__version__"]
