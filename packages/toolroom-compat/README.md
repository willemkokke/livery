<!-- Seeded from the template channel (package-python kind) at
     birth; this file is the workspace's own. Edit it directly:
     the template never rewrites it.
-->
# toolroom

The toolroom distribution, now a thin shim over livery-toolroom.
It keeps `import toolroom` working and continues the old release
line; the real package is
[livery-toolroom](https://pypi.org/project/livery-toolroom/).
