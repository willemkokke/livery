[build-system]
requires = ["uv_build>=0.11,<0.13"]
build-backend = "uv_build"

[project]
name = "livery-workshop"
version = "0.1.0"
description = "The livery ecosystem's devkit: the task surface, layers, templates, and content."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.11"
authors = [{ name = "Willem Kokke", email = "mail@willem.net" }]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Typing :: Typed",
]
dependencies = [
    "livery-forge>=0.1.0",
    # The floors on deps the dev toolchain also carries align with
    # the lock: the release train's floor leg shares one venv with
    # the locked toolchain, so a floor below the toolchain's pin is
    # a claim the leg cannot test (its movement guard refuses the
    # silent un-starving). footman 0.50 is a real requirement
    # besides: the expose axis and the runtime brand paths are its.
    "footman>=0.50",
    "toolroom>=0.6.1",
    # The template engine behind `fm template.check` and `fm
    # new.package`; pyyaml reads the adopted answers files. The
    # pyyaml floor is the oldest release whose wheels cover every
    # Python this package supports: the release train's floor leg
    # installs the floor itself, and 6.0.0's sdist no longer builds
    # under a current Cython.
    "copier>=9.17.2",
    "pyyaml>=6.0.3",
]

[project.entry-points."footman.tasks"]
"livery.workshop" = "livery.workshop._tasks"

# The devkit advertisement: "meant to be mounted as built-in tasks".
# `fm self.add livery-workshop` records the name; whether it mounts
# stays the machine owner's call through builtins.discovery_mode.
[project.entry-points."footman.builtin"]
"livery.workshop" = "livery.workshop._tasks"

[project.urls]
Repository = "https://github.com/willemkokke/livery"

[tool.uv.build-backend]
# PEP 420 namespace: the importable package is livery.workshop and
# there is no livery/__init__.py anywhere in this repository.
module-name = "livery.workshop"
