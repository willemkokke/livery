# Rendered by the template channel ({{ template_source_label }}, project kind);
# the gate keeps it matching its render. Edit the source and
# run `{{ runner_prog }} template.apply`; an edit here is drift.
dist/
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.coverage*
fm-profile.json
fm-gate.json
.claude/worktrees/
.mypy_cache/
.forge.dev.env
.workshop/
typings/
.repo.env.local
site/
zensical.toml
.docs-build/
docs/packages/
docs/releases/
docs/tasks/
docs/tools/
docs/_generated/
packages/*/src/**/_docs/
.docs-preview/
packages/*/docs/_generated/
packages/*/_generated/
packages/*/htmlcov/
coverage-data/
# Finder writes .DS_Store into any directory it opens; a workflow verb
# refuses a dirty tree, so the file is never a change.
.DS_Store
