<!-- Seeded from the template channel (package-base kind) at
     birth; this file is the workspace's own. Edit it directly:
     the template never rewrites it.
-->
# footman

The footman distribution, now a thin shim over livery-footman.
Every `footman.*` import forwards to `livery.footman` whole. New
code imports `livery.footman` directly.
