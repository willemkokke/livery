<!-- Seeded from the template channel (package-python kind) at
     birth; this file is the workspace's own. Edit it directly:
     the template never rewrites it.
-->
# footman

The footman distribution, now a thin shim over livery-footman.
It keeps `import footman` working and continues the old release
line; the console scripts and entry points ship with
[livery-footman](https://pypi.org/project/livery-footman/).
