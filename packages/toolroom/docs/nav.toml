# This package's section of the workspace site, hand-authored.
# Labels map to pages relative to this docs/ tree, or to nested
# lists. Every authored page must appear here, and every entry must
# name a real page; the changelog and API entries are appended by
# the emitter. The tool entries are authored with the curated
# driver set: `fm toolroom.pages` writes the pages, and a driver
# added without its entry goes red in the strict build.
nav = [
    { "Index" = "index.md" },
    { "Playground" = "playground.md" },
    { "Using the tools" = "usage.md" },
    { "With footman" = "footman.md" },
    { "Testing" = "testing.md" },
    { "Typing and the stubs" = "typing.md" },
    { "Colour" = "_generated/colour.md" },
    { "API reference" = "api.md" },
    { "Tools" = [
        { "The curated tools" = "_generated/tools/index.md" },
        # nav:begin tools
        # nav:end tools
    ] },
]
